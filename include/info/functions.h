#pragma once


#include "Runtime.h"
#include "Schema.h"
#include "Instance.h"

namespace info {
  InstanceRef createSchemaInstance(Runtime& runtime, Schema& schema, const std::string& componentId) {
    return nullptr;
  }
}